import { css } from 'styled-components';

const container = css`
  margin: auto;
  padding-right: 20px;
  padding-left: 20px;
  max-width: 1140px;
`;

const mixins = {
  container
};

export default mixins;
